apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-svc
spec:
  selector:
    matchLabels:
      app: search-svc
  template:
    metadata:
      labels:
        app: search-svc
    spec:
      containers:
        - name: search-svc
          image: henriquequeirozcunha/search-svc
          imagePullPolicy: Never
          env:
            - name: RabbitMq__Host
              value: rabbit-cluster-ip
            - name: RabbitMq__Username
              value: rabbit
            - name: RabbitMq__Password
              value: rabbit
            - name: ConnectionStrings__MongoDbConnection
              value: mongodb://root:mongopw@mongo-clusterip # NOTE: "@host" is taking IP address from mongo-clusterip CLUSTER IP SERVICE
            - name: AuctionServiceUrl
              value: http://auction-clusterip

---
apiVersion: v1
kind: Service
metadata:
  name: search-clusterip
spec:
  type: ClusterIP
  selector:
    app: search-svc
  ports:
    - port: 80
      targetPort: 80
